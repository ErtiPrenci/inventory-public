name: CI/CD Backend - Deploy to AWS Lambda

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v4
        with:
          go-version: "1.21"
          cache: true

      - name: Complete CI/CD
        run: echo "✅ CI/CD completed successfully"

      # CI/CD Deploy project in AWS

      # - name: Build Lambda with Makefile
      #   run: make build-lambda

      # - name: Setup Terraform
      #   uses: hashicorp/setup-terraform@v3
      #   with:
      #     terraform_wrapper: false

      # - name: Verify Variables Presence
      #   run: |
      #     echo "--- Verificando Presencia de Credenciales ---"
      #     if [ -z "${{ secrets.AWS_ACCESS_KEY_ID }}" ]; then echo "❌ AWS_ACCESS_KEY_ID is EMPTY"; else echo "✅ AWS_ACCESS_KEY_ID is set"; fi
      #     if [ -z "${{ secrets.AWS_SECRET_ACCESS_KEY }}" ]; then echo "❌ AWS_SECRET_ACCESS_KEY is EMPTY"; else echo "✅ AWS_SECRET_ACCESS_KEY is set"; fi

      #     echo "--- Verificando Variables de Terraform ---"
      #     if [ -z "${{ secrets.DB_URL }}" ]; then echo "❌ DB_URL is EMPTY"; else echo "✅ DB_URL is set (Length: ${#DB_URL})"; fi
      #     echo "Environment: ${{ vars.ENVIRONMENT || 'prod' }}"
      #     echo "Region: sa-east-1"

      # - name: Terraform Init
      #   working-directory: ./terraform
      #   run: terraform init
      #   env:
      #     AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      #     AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      #     AWS_REGION: "sa-east-1"

      # - name: Terraform Plan
      #   working-directory: ./terraform
      #   run: |
      #     terraform plan \
      #       -var="db_url=${{ secrets.DB_URL }}" \
      #       -var="jwt_secret=${{ secrets.JWT_SECRET }}" \
      #       -var="domain_name=${{ vars.DOMAIN_NAME }}" \
      #       -var="subdomain=${{ vars.SUBDOMAIN }}" \
      #       -var="company_name=${{ vars.COMPANY_NAME }}" \
      #       -var="environment=prod" \
      #       -out=tfplan
      #   env:
      #     AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      #     AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      #     AWS_REGION: "sa-east-1"

      # - name: Terraform Apply
      #   if: github.ref == 'refs/heads/main'
      #   working-directory: ./terraform
      #   run: terraform apply -auto-approve tfplan
      #   env:
      #     AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      #     AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
